---
import Base from '../layouts/Base.astro';
import T from '../components/T.astro';
import ReviewCard from '../components/ReviewCard.astro';
import { getReviews } from '../lib/content';
import { t } from '../i18n/ui';
import { SHOW_REVIEWS } from '../config/launch';

const reviews = SHOW_REVIEWS ? await getReviews() : [];
---
<Base title={t('reviews.title', 'en')} active="reviews" description={t('reviews.subtitle', 'en')}>
  <div class="wrap">
    <section class="sec">
      <h1 class="lead-title"><T k="reviews.title" /></h1>
      <T k="reviews.subtitle" tag="p" class="t-body mt-8" />
    </section>

    <section class="sec" style="padding-top:6px;">
      {reviews.length > 0 ? (
        <div class="grid-reviews">
          {reviews.map((r) => <ReviewCard review={r} />)}
        </div>
      ) : (
        <div class="empty-state">
          <p class="card-title" data-i18n="reviews.empty.title">{t('reviews.empty.title', 'en')}</p>
          <p class="t-body" data-i18n="reviews.empty.body">{t('reviews.empty.body', 'en')}</p>
        </div>
      )}
    </section>

    <section class="sec">
      <div class="cta-band">
        <a class="btn pri full" href="/contact" data-i18n="reviews.cta" data-event="cta:reviews">{t('reviews.cta', 'en')}</a>
      </div>
    </section>
  </div>
</Base>
