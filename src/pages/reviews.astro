---
import Base from '../layouts/Base.astro';
import T from '../components/T.astro';
import ReviewCard from '../components/ReviewCard.astro';
import { getReviews } from '../lib/content';
import { defaultLang, t } from '../i18n/ui';
import { SHOW_REVIEWS } from '../config/launch';

const reviews = SHOW_REVIEWS ? await getReviews() : [];
---
<Base title={t('reviews.title', defaultLang)} active="reviews" description={t('reviews.subtitle', defaultLang)}>
  <div class="wrap">
    <section class="sec">
      <h1 class="lead-title"><T k="reviews.title" /></h1>
      <T k="reviews.subtitle" tag="p" class="t-body mt-8" />
    </section>

    <section class="sec" style="padding-top:6px;">
      {reviews.length > 0 ? (
        <div class="grid-reviews">
          {reviews.map((r) => <ReviewCard review={r} />)}
        </div>
      ) : (
        <div class="empty-state">
          <p class="card-title" data-i18n="reviews.empty.title">{t('reviews.empty.title', defaultLang)}</p>
          <p class="t-body" data-i18n="reviews.empty.body">{t('reviews.empty.body', defaultLang)}</p>
        </div>
      )}
    </section>

    <section class="sec">
      <div class="cta-band">
        <a class="btn pri full" href="/contact#dm" data-i18n="dm.cta" data-event="cta:reviews">{t('dm.cta', defaultLang)}</a>
      </div>
    </section>
  </div>
</Base>
